
<%= link_to "Host an event", new_event_path %>  <br>

<% @events.each do |event| %>
---------------------------------- <br>
  <%= link_to event.name, event_path(event) %> <br />
  <%= event.city %> <%= event.state %>, <%= event.zip %> <br /> <br />

<% end %>

<%= link_to 'Previous page', page: @page - 1%>
<%= link_to 'Next page', page: @page + 1%>
