Active Users

<% @active.each do |user| %>
  <br> <%= user.username %> <br>
<% end %>


<script>
  $(document).ready(function() {
    if (window.activeInterval === undefined) {
      console.log('creating interval');
      window.activeInterval = setInterval(function() {
        console.log('tick');
        jQuery.ajax({
          url: "chat",
          type: "GET",
          dataType: "script"
        });
      }, 60000);
    }
  });
</script>
