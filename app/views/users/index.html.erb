<h1>Local Users</h1>

<% @users.each do |user| %>
  <div class="user">
    <%= image_tag user.profile_img.url(:thumb), alt: "#{user.username} avatar" %>
    <%= link_to  user.username, "users/#{user.id}"%>
  </div>
<% end %>

<%= link_to 'Previous page', page: @page - 1%>
<%= link_to 'Next page', page: @page + 1%>
